# API
API_PORT=8000

# Groq API key (LLM baseline via OpenAI-compatible client; also used by /summarize if pointed at OpenAI)
OPENAI_API_KEY=
# PostgreSQL
POSTGRES_USER=copilot
POSTGRES_PASSWORD=copilot
POSTGRES_DB=copilot
POSTGRES_PORT=5432

# Redis
REDIS_PORT=6379

# MinIO
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin
MINIO_PORT=9000
MINIO_CONSOLE_PORT=9001
MINIO_BUCKET=copilot

# Vault (dev mode only — not for production)
VAULT_DEV_ROOT_TOKEN_ID=dev-root-token
VAULT_PORT=8200
