# Legacy alias kept for backward compatibility.
# Prefer:
#   - requirements-runtime.txt  (API container, CPU inference)
#   - requirements-ml.txt       (training + evals)
#
# Note: this file does NOT pre-install CPU torch. For Linux deployments use
# the Dockerfile (which pins the CPU wheel index) instead of this file.
-r requirements-runtime.txt
-r requirements-ml.txt
